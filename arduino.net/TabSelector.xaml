<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:arduino.net" x:Class="arduino.net.TabSelector" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="250">
	<UserControl.Resources>
		<DataTemplate x:Key="TabsItemTemplate">
			<Grid x:Name="grid" HorizontalAlignment="Stretch" Height="31">
				<TextBlock Text="{Binding Header}" VerticalAlignment="Center" Foreground="{DynamicResource C0}"/>
			</Grid>
			<DataTemplate.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" TargetName="grid" Value="{DynamicResource C2}"/>
				</Trigger>
			</DataTemplate.Triggers>
		</DataTemplate>
		
		
	</UserControl.Resources>
	
    <Grid>
        <ListBox x:Name="TabItemsListBox" VerticalAlignment="Top" ItemTemplate="{DynamicResource TabsItemTemplate}" BorderThickness="0" Background="{x:Null}" />
    </Grid>
</UserControl>
