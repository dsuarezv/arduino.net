<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:arduino.net" x:Class="arduino.net.MainWindow"
        Title="MainWindow" Height="609" Width="809"
        WindowStyle="None" ResizeMode="CanResizeWithGrip" Loaded="Window_Loaded">
    <Grid Width="auto">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>


        <TextBlock x:Name="TitleLabel" 
                Grid.Row="0"
			    Grid.Column="0" 

                Padding="10,5"
			    FontWeight="Light"
			    FontSize="36"
			    Foreground="#FFA1A1A1" ><Run Text="Blink sample - arduino.net"/></TextBlock>

        <local:SystemButtons
            Grid.Row="0"
			Grid.Column="0" 
            HorizontalAlignment="Right"
            VerticalAlignment="Top"/>


        <Border Grid.Row="1" Padding="10,0,10,10">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="BuildButton" Content="build" Click="BuildButton_Click"/>
                <Button x:Name="DeployButton" Content="deploy" Click="DeployButton_Click"/>
                <Button x:Name="DebugButton" Content="debug" Click="DebugButton_Click"/>
            </StackPanel>
        </Border>

        <local:StatusHeaderControl 
            x:Name="StatusControl"
            Width="auto"
            Grid.Row="2" 
            HorizontalAlignment="Left"/>

        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280px"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <GridSplitter 
				Grid.Column="0"
				HorizontalAlignment="Right"
				VerticalAlignment="Stretch"
				Background="#b6b6b6" 
				ShowsPreview="True"
				Width="1"/>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="150px"/>
                </Grid.RowDefinitions>

                <GridSplitter
					Grid.Row="0"
					HorizontalAlignment="Stretch"
					VerticalAlignment="Bottom"
					Background="#b6b6b6" 
					ShowsPreview="True"
					Height="1"/>



                <local:CodeTextBox x:Name="SampleCodeBox" Grid.Row="0" Margin="0, 0, 0, 1"/>



            </Grid>
        </Grid>
    </Grid>
</Window>
